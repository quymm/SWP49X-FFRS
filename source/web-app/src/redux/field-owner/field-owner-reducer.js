export const fieldOwnerReducer = (state, action) => {
    switch (action.type) {
        case 'LOGIN':
            return({});
        case 'REGISTER':
            return({});
        default:
            return state;
    }
}